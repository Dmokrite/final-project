# Use postgres/example user/password credentials
version: '3.9'

services:
  postgres:
    image: postgres:latest
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: postmdp
      POSTGRES_DB: comixdb
